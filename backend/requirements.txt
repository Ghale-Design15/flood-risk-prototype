fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
joblib==1.4.2
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.6.1
xgboost==2.1.1
# LSTM serving. The saved model is Keras 3.15.1 — this must be a TensorFlow whose
# bundled Keras can load that format. Confirm/adjust to match Ghale's training env.
tensorflow-cpu==2.18.0
# Alert audit log (Supabase) + email dispatch (SendGrid).
supabase==2.7.4
sendgrid==6.11.0
pytest==8.2.2
httpx==0.27.0
